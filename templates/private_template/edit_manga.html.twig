<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="img/logo-kword.png" type="image/x-icon">
    <link rel="stylesheet" href="../css/tailwind.css">
    <link rel="stylesheet" href="../css/main.css">
    <title>Admin K-WORLD</title>
</head>
<body class="flex flex-col overflow-x-hidden">
<header class="fixed z-50 top-0 w-full flex flex-col text-sm font-medium">

    <div class="w-full h-screen absolute flex items-center justify-center p-2" style="background: rgba(0, 0, 0, .5);">
        <div class="w-full max-w-xl px-4 py-12 kw-bg-yellow-trans shadow-xl rounded-xl">
            <h2 class="font-medium text-2xl md:text-4xl text-white text-center pb-6">Editer les informations du manga</h2>

            <div class="p-2 md:flex-1">
            {# pass a string argument to get only the messages of that type #}
            {% for message in app.flashes('success') %}
                <p class="py-3 text-white text-center text-lg" style="color:red">
                    {{ message }}
                </p>
            {% endfor %}
            {{ form_start(form ,{ attr :{'class':'flex flex-col space-y-5'}}) }}
            <div class="flex flex-col space-y-1 text-white">
                {{ form_label(form.titreManga) }}
                {{ form_widget(form.titreManga,{ 'attr' : { 'class':'px-4 py-2 transition duration-300 border border-gray-300 bg-transparent rounded focus:border-transparent focus:outline-none focus:ring-4 focus:ring-blue-200','row':'5' } }) }}
            </div>
            <div class="flex flex-col space-y-1 text-white">

                {{ form_label(form.AuteurManga) }}
                {{ form_widget(form.AuteurManga,{ 'attr': {
                    'class':'px-4 py-2 transition duration-300 border border-gray-300 bg-transparent rounded focus:border-transparent focus:outline-none focus:ring-4 focus:ring-blue-200',
                    'row':5
                } }) }}
            </div>
            <div class="flex flex-col space-y-1 text-white">
                {{ form_label(form.statusManga) }}
                {{ form_widget(form.statusManga,{ 'attr': {
                    'class':'px-4 py-2 transition duration-300 border border-gray-300 bg-transparent rounded focus:border-transparent focus:outline-none focus:ring-4 focus:ring-blue-200',
                    'row':5
                } }) }}
            </div>
            <div>
                {{ form_widget(form.Enregistrer,{ 'attr': {'class':'w-full px-4 py-2 text-lg font-semibold text-white kw-bg-yellow rounded-md shadow transform hover:scale-105 transition-all duration-500 focus:outline-none'} }) }}
            </div>
            {{ form_end(form) }}
        </div>
        </div>
    </div>
</header>

<!-- Scripts -->

<!-- Alpine.js -->
<script src="js/alpine.js" defer></script>
</body>
</html>
